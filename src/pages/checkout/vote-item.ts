export class VoteItem {
  title: string;
  company: string;
  category: string;

  constructor(title: string, company: string, category: string) {
    this.title = title;
    this.company = company;
    this.category = category;
  }
}
